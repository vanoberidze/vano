#include <iostream>

void printRange(int start, int end) {
    for (int i = start; i <= end; i++) {
        std::cout << i << " ";
    }
    std::cout << std::endl;
}

int main() {
    int number;

    std::cout << "Enter an integer: ";
    std::cin >> number;

    printRange(number, number + 10);

    return 0;
}

